다형성(polymorphism)
- 상속 개념과 연관이 깊다
- 여러가지 형태를 가질 수 있는 능력
- 조상 클래스 타입의 참조변수로 자손 클래스의 인스턴스 참조

- 캐스팅
  자손타입은 조상타입으로 자동 형변환 가능
  조상타입은 자손타입으로 강제 형변환 해야 함

- 매개변수의 다형성

Vehicle v = new Bus();
v가 접근할 수 있는 범위 제한

Bus bus = new Bus();
상속 시 메소드 오버라이딩을 하면 부모 메소드는 가려지고 자식 메소드만 실행